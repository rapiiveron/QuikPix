<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quikpix">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" 
        android:minSdkVersion="33" />
    
    <!-- For Android 14 partial access -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" 
        android:minSdkVersion="34" />
    
    <!-- For loading sample images -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FastGallery"
        android:hardwareAccelerated="true">

        <activity
            android:name=".presentation.SimpleCategoriesActivity"
            android:exported="true"
            android:theme="@style/Theme.FastGallery">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.CategoryDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.FastGallery">
        </activity>
        <activity
            android:name=".presentation.Phase3ReadyActivity"
            android:exported="false"
            android:theme="@style/Theme.FastGallery">
        </activity>
    </application>
</manifest>